# ==============================================================================
# QUANTNUIS EC2 API - LIGHTWEIGHT DEPENDENCIES
# ==============================================================================
# No TensorFlow/ML dependencies - much smaller footprint (~50MB vs ~500MB)
# For the stateful API running on EC2 with PostgreSQL
# ==============================================================================

# --- WEB FRAMEWORK ---
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.9

# --- DATABASE ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9  # PostgreSQL driver

# --- SECURITY ---
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-jose[cryptography]>=3.3.0

# --- VALIDATION ---
pydantic>=2.0.0
email-validator>=2.0.0

# --- DATA PROCESSING (for annotations) ---
# Minimal audio processing for slicing annotations
pydub>=0.25.0
soundfile>=0.12.0

# --- UTILITIES ---
python-dotenv>=1.0.0

# --- RATE LIMITING ---
slowapi>=0.1.9

# --- HTTP CLIENT (for GitHub API) ---
httpx>=0.27.0
